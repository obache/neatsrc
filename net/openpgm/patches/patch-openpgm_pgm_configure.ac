$NetBSD$

* NetBSD atomic_ops support

--- openpgm/pgm/configure.ac.orig	2012-12-04 02:57:22.000000000 +0000
+++ openpgm/pgm/configure.ac
@@ -294,6 +294,8 @@ AC_COMPILE_IFELSE(
 /* Solaris intrinsic */
 #elif defined( __APPLE__ )
 /* Darwin intrinsic */
+#elif defined( __NetBSD__ )
+/* NetBSD intrinsic */
 #elif defined( __GNUC__ ) && ( __GNUC__ * 100 + __GNUC_MINOR__ >= 401 )
 /* GCC 4.0.1 intrinsic */
 #elif defined( _WIN32 )
