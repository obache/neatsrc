$NetBSD$

* regen 

--- configure.orig	2015-07-16 08:50:25.000000000 +0000
+++ configure
@@ -17247,7 +17247,7 @@ fi
 	   ;;
        # list of supported OS cores that do not use libapm
        i386-*-freebsd*|*-*-netbsd*|*-*-openbsd*|*-*kfreebsd*-gnu)
-          if  -n "${OS_SYS}" ; then
+          if test -n "${OS_SYS}"; then
 	       ACPIINC="-I${OS_SYS}"
 	   else
 	       ACPIINC="-I/usr/src/sys"
