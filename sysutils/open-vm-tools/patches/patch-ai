$NetBSD: patch-ai,v 1.2 2008/08/31 06:36:47 scottr Exp $

--- lib/file/fileIOPosix.c.orig	2015-06-17 16:14:30.000000000 +0000
+++ lib/file/fileIOPosix.c
@@ -78,7 +78,7 @@
 #include <dlfcn.h>
 #include <sys/xattr.h>
 #else
-#if defined(__FreeBSD__)
+#if defined(__FreeBSD__) || defined(__NetBSD__)
 #include <sys/param.h>
 #include <sys/mount.h>
 #else
@@ -1821,7 +1821,7 @@ FileIO_Writev(FileIODescriptor *fd,  // 
 
 
 #if defined(__linux__) || defined(__APPLE__) || defined(__FreeBSD__) ||\
-    defined(sun)
+    defined(sun) || defined(__NetBSD__)
 
 /*
  *----------------------------------------------------------------------
@@ -2009,7 +2009,7 @@ exit:
 }
 
 #endif /* defined(__linux__) || defined(__APPLE__) || defined(__FreeBSD__) ||
-          defined(sun) */
+          defined(sun) || defined(__NetBSD__) */
 
 
 #if defined(__linux__)
@@ -2270,7 +2270,7 @@ FileIOPwritevInternal(FileIODescriptor *
 
 
 #if defined(__linux__) || defined(__APPLE__) || defined(__FreeBSD__) ||\
-    defined(sun)
+    defined(sun) || defined(__NetBSD__)
 
 /*
  *----------------------------------------------------------------------
@@ -2364,7 +2364,7 @@ FileIO_Pwritev(FileIODescriptor *fd,   /
 }
 
 #endif /* defined(__linux__) || defined(__APPLE__) || defined(__FreeBSD__) ||
-          defined(sun) */
+          defined(sun) || defined(__NetBSD__) */
 
 
 /*
