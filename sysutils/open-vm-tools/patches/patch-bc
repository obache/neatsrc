$NetBSD: patch-bc,v 1.2 2008/08/31 06:36:48 scottr Exp $

--- lib/misc/posixPosix.c.orig	2015-06-17 16:14:30.000000000 +0000
+++ lib/misc/posixPosix.c
@@ -47,7 +47,7 @@
 #include <sys/param.h>
 #include <sys/mount.h>
 #include <CoreFoundation/CoreFoundation.h>
-#elif defined(__FreeBSD__)
+#elif defined(__FreeBSD__) || defined(__NetBSD__)
 #include <sys/param.h>
 #include <sys/mount.h>
 #elif defined(sun)
@@ -1605,7 +1605,11 @@ Posix_Putenv(Unicode name)  // IN:
 
 int
 Posix_Statfs(ConstUnicode pathName,     // IN:
+#if defined(__NetBSD__)
+             struct statvfs *statfsbuf) // IN:
+#else
              struct statfs *statfsbuf)  // IN:
+#endif
 {
    char *path;
    int ret;
@@ -1614,7 +1618,11 @@ Posix_Statfs(ConstUnicode pathName,     
       return -1;
    }
 
+#if defined(__NetBSD__)
+   ret = statvfs(path, statfsbuf);
+#else
    ret = statfs(path, statfsbuf);
+#endif
 
    free(path);
 
@@ -1745,7 +1753,7 @@ Posix_Unsetenv(ConstUnicode name)  // IN
 
 #if !defined(sun) // {
 
-#if !defined(__APPLE__) && !defined(__FreeBSD__) // {
+#if !defined(__APPLE__) && !defined(__FreeBSD__) && !defined(__NetBSD__) // {
 /*
  *----------------------------------------------------------------------
  *
