$NetBSD: patch-bd,v 1.1 2008/08/31 06:36:48 scottr Exp $

--- lib/misc/util_misc.c.orig	2015-06-17 16:14:30.000000000 +0000
+++ lib/misc/util_misc.c
@@ -51,7 +51,7 @@
 #  include <netinet/in.h>    // for INET6_ADDRSTRLEN
 #endif
 
-#if defined(__APPLE__) || defined(__FreeBSD__)
+#if defined(__APPLE__) || defined(__FreeBSD__) || defined(__NetBSD__)
 #include <pthread.h>
 #endif
 
@@ -512,7 +512,7 @@ Util_GetCurrentThreadId(void)
    ASSERT(tid != (pid_t)-1);
 
    return tid;
-#elif defined(__APPLE__) || defined(__FreeBSD__)
+#elif defined(__APPLE__) || defined(__FreeBSD__) || defined(__NetBSD__)
    ASSERT_ON_COMPILE(sizeof(Util_ThreadID) == sizeof(pthread_t));
 
    return pthread_self();
